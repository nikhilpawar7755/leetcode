bool isValid(char c){
    if (c.toLower() >= 'a' && c.toLower() <= 'z'){
        return true;
    }else if (atoi(c) >= 0 && atoi(c) <= 9){
        return true;
    }
    return false;
}

bool isPalindrome(char * s){
    int length = strlen(s);
    int j = length -1;
    for (int i =0 ; i < length; i++){
        if 
    }
}
